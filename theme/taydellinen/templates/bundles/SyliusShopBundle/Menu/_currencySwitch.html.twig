{% if currencies|length > 1 %}
    <div class="hidden lg:block dropdown" id="sylius-currency-selector" {{ sylius_test_html_attribute('currency-selector') }}>
        <button class="btn btn-ghost" {{ sylius_test_html_attribute('active-currency') }}>{{ active }}</button>
        <ul tabindex="0" class="dropdown-content menu p-2 shadow bg-base-100">
            {% for code in currencies %}
                {% if code is not same as(active) %}
                    <li>
                        <a
                            href="{{ path('sylius_shop_switch_currency', {'code': code}) }}"
                            {{ sylius_test_html_attribute('available-currency') }}
                        >
                            {{ code }}
                        </a>
                    </li>
                {% endif %}
            {% endfor %}
        </ul>
    </div>
{% endif %}
